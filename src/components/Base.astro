<nav>
  <div class="maindiv">
    <div class="overlay"></div>
    <div class="leftdiv animate-slide-up">
      <h1 class="pHeading">Сухофрукты и орехи<br />оптом по всей Самаре</h1>
      <p class="subtext">Натурально. Оптово. Доступно.</p>
      <div class="contforbutton">
        <a href="/catalog" class="but_cat">Смотреть каталог</a>
        <a href="/AboutUs" class="but_comp">О компании</a>
      </div>
    </div>
  </div>
</nav>


<style>
@import url('https://fonts.googleapis.com/css2?family=Raleway:wght@300;500;700&display=swap');

body {
  margin: 0;
  font-family: 'Raleway', sans-serif;
  background-color: #ffffff;
}

nav {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: 10px;
}

.maindiv {
  position: relative;
  height: 550px;
  width: 90%;
  max-width: 1300px;
  border-radius: 15px;
  overflow: hidden;
  background-image: url('/images/meshki.jpg');
  background-size: cover;
  background-position: center;
}

.overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.4);
}

.leftdiv {
  position: relative;
  background-color: rgb(234, 169, 94);
  width: 40%;
  margin: 60px;
  padding: 40px;
  border-radius: 20px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  box-shadow: 8px 8px 15px rgba(0, 0, 0, 0.2);
}

.pHeading {
  font-size: 42px;
  font-weight: 700;
  color: #ffffff;
  margin: 0;
  line-height: 1.2;
}

.subtext {
  font-size: 18px;
  color: #6b6b6b;
  margin: 0;
}

.contforbutton {
  display: flex;
  gap: 20px;
  margin-top: 30px;
}

.contforbutton a {
  padding: 14px 24px;
  font-size: 16px;
  border-radius: 8px;
  text-decoration: none;
  transition: all 0.3s ease;
  font-weight: 500;
}

.but_cat {
  background-color: #5a1f1f;
  color: white;
}

.but_cat:hover {
  background-color: #7a2a2a;
}

.but_comp {
  background-color: transparent;
  border: 2px solid #5a1f1f;
  color: #5a1f1f;
}

.but_comp:hover {
  background-color: #5a1f1f;
  color: white;
}

@keyframes slide-up {
  from {
    transform: translateY(40px);
    opacity: 0;
  }
  to {
    transform: translateY(0px);
    opacity: 1;
  }
}

.animate-slide-up {
  animation: slide-up 1s ease-out;
}

/* Адаптив */
@media (max-width: 1024px) {
  .leftdiv {
    width: 80%;
    margin: 30px auto;
    padding: 25px;
  }
  .pHeading {
    font-size: 32px;
  }
}

@media (max-width: 480px) {
  .maindiv {
    height: 400px;
  }
  .leftdiv {
    width: 90%;
    margin: 20px auto;
    padding: 20px;
  }
  .pHeading {
    font-size: 24px;
  }
  .subtext {
    font-size: 14px;
  }
  .contforbutton {
    flex-direction: column;
    align-items: stretch;
  }
}

</style>